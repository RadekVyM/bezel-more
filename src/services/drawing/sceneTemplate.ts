import { SceneTemplate } from '../../types/SceneTemplate'
import { Size } from '../../types/Size'

export async function drawSceneTemplate(context: CanvasRenderingContext2D, template: SceneTemplate, size: Size) {
    
}